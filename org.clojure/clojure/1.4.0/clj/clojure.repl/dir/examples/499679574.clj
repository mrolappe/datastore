(require 'clojure.string 'clojure.repl)

(clojure.repl/dir clojure.string)
;; Prints
;; > blank?
;; > capitalize
;; > escape
;; > join
;; > lower-case
;; > replace
;; > replace-first
;; > reverse
;; > split
;; > split-lines
;; > trim
;; > trim-newline
;; > triml
;; > trimr
;; > upper-case
;;
;; => nil
